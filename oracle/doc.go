// Package oracle implements the utility that creates random numbers,
// or samples static random numbers that can be consumed through
// oracle doubles in order to test deterministic results from objects
// that use such resources.

package oracle
